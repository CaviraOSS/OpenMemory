# MCP

MCP support is deferred and no longer ships in the active `packages/openmemory-js`
source tree.

The default JavaScript server does not register `/mcp`, and the `opm` CLI no
longer starts an MCP stdio server.

Rebuild MCP later against durable `/v1` remember, recall, and explain semantics.
